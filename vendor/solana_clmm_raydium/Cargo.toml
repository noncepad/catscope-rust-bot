[package]
name = "solana-clmm-raydium"
version = "0.3.0"
edition = "2021"
description = "Raydium CLMM pure-Rust swap math (vendored: no Anchor/tokio/network)."

[lib]
name = "solana_clmm_raydium"
path = "src/lib.rs"

[dependencies]
uint = { version = "0.9" }
